class Text{
    static global_X = 0;
    static text_height = 80;
    constuctor(text, typ){

    }
}

function setup(){
    createCanvas(windowWidth, windowHeight);
}

function draw(){
    background(255);

}